spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:mydb

server:
  port: 8081

bot:
  name: "test_bot_name"
  token: "test_bot_token"

jira:
  link: "https://test.com/"
  dashboards: "TEST1"

settings:
  number-of-review-stages: 2

camunda:
  bpm:
    admin-user:
      id: admin
      password: admin