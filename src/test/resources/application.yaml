spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}


server:
  port: 8081

bot:
  name: "test_bot_name"
  token: "test_bot_token"

jira:
  link: "https://test.com/"
  dashboards: "TEST1"

camunda:
  bpm:
    admin-user:
      id: admin
      password: admin